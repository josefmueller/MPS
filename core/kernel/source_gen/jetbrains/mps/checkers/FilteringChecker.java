package jetbrains.mps.checkers;

/*Generated by MPS */

import jetbrains.mps.errors.item.ReportItem;
import java.util.function.Predicate;
import org.jetbrains.mps.openapi.module.SRepository;
import org.jetbrains.mps.openapi.util.Consumer;
import org.jetbrains.mps.openapi.util.ProgressMonitor;

public class FilteringChecker<O, I extends ReportItem> implements IChecker<O, I> {
  private IChecker<O, I> myOrigin;
  private Predicate<I> myAccept;
  public FilteringChecker(IChecker origin, Predicate<I> accept) {
    myOrigin = origin;
    myAccept = accept;
  }
  @Override
  public void check(O toCheck, SRepository repository, final Consumer<I> errorCollector, ProgressMonitor monitor) {
    Consumer<I> consumer = new Consumer<I>() {
      public void consume(I item) {
        if (myAccept.test(item)) {
          errorCollector.consume(item);
        }
      }
    };
    myOrigin.check(toCheck, repository, consumer, monitor);
  }
}
